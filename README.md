# Android_DatabaseSample

This is simple example application for android database systems. 

process :-
       - make a database
       - add data(insert)
       - view data(select)
       - update data
       - delete data(working on it)
